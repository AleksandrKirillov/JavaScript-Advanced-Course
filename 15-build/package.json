{
  "name": "book-app",
  "version": "1.0.0",
  "description": "HomeWork",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup index.js --format umd --name 'myBundle' --file bundle.js",
    "start": "live-server"
  },
  "keywords": [
    "test"
  ],
  "author": "AK",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2",
    "rollup": "^4.3.0"
  }
}
